extends layout

block content
  p #{tapes.length} Results
  each tape in tapes
    a(href="/tapes/" + tape.conversationCode) #{tape.conversationCode}
    p Start Date: #{tape.startDateTime}
    p Description: #{tape.description}
